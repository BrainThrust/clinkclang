export interface Post {
  title: string;
  slug: string;
  date: string;
  content: string;
  description?: string;
} 

export interface Docs {
  title: string;
  slug: string;
  content: string;
  description?: string;
}